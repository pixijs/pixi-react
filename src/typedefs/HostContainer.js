/**
 * @typedef {object} BaseHostContainer
 * @property {import('./Node.js').Node | null} head
 */

/**
 * @typedef {import('pixi.js').Application & BaseHostContainer} HostContainer
 * @property {import('./Node.js').Node | null} head
 */
export const HostContainer = {};
