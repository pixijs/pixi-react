import { Canvas, Meta, Story } from '@storybook/addon-docs';
import * as PIXI from 'pixi.js'
import { Stage, Container } from '../../docz-rp'
import BitmapText from './BitmapTextDocz'

<Meta title="Components/BitmapText" component={BitmapText} />

export const Template = (args) => (
  <Stage width={500} height={300} options={{ backgroundColor: 0xffffff }}>
    <BitmapText anchor={0.5} x={250} y={150} text="Hello World!" style={{ fontName: 'Desyrel', fontSize: 50 }}/>
  </Stage>
)

# BitmapText

## Props

[https://pixijs.download/dev/docs/PIXI.BitmapText.html](https://pixijs.download/dev/docs/PIXI.BitmapText.html)

## Usage

<Canvas>
  <Story name="default">{Template.bind({})}</Story>
</Canvas>

## Example

Make sure to load the bitmap font data before mounting.

See https://pixijs.io/examples/#/demos/text-demo.js

```js
import { createRoot } from 'react-dom'
import { Stage, BitmapText } from '@inlet/react-pixi'

PIXI.Loader.shared
  .add('desyrel', './assets/desyrel.xml')
  .load(onAssetsLoaded)

const App = () => (
  <Stage>
    <BitmapText text="Hello World" style={{ fontName: 'Desyrel', fontSize: 35 }}>
  </Stage>
)

const container = document.getElementById('root')
const root = createRoot(container)

function onAssetsLoaded() {
  root.render(<App />)
}
```
